define([
    'jquery',
    'underscore',
    'backbone',
    'router'
],function ($, _, Backbone) {
    var Category = new Backbone.Model.extend({
        defaults: {

        }

    });

    return Category;
});